#include<vector>
using namespace std;
int main()
 {
    int T;
    cin>>T;
	for(int i = 0;i < T; i++)
	{
	    vector<int> v;
	    int n;
	    cin>>n;
	    for(int i = 0; i < n; i++)
	    {
	        int ip;
	        cin>>ip;
	        v.push_back(ip);
	    }
	    int d;
	    cin>>d;
	    for(int i =d; i< n; i++)
	    {
	        cout<<v[i]<<" ";
	    }
	    for(int i = 0; i<d; i++)
	    {
	        cout<<v[i]<<" ";
	    }
	    cout<<endl;
	}
	return 0;
}
